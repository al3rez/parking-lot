language: go
go:
  - 1.12.7
before_install:
  - go get -t -v ./...
script:
  - go test -race -coverprofile=coverage.txt github.com/azbshiri/parking-lot
after_success:
  - bash <(curl -s https://codecov.io/bash)
